Repl:

> LET NUM_ARRAY = [12, 78, 24, 10]
> LET ARR_LEN = | GET FROM NUM_ARRAY LEN |
> LET I = 0
> LET SUM = 0
> WHILE I < ARR_LEN START
	> LET CURRENT_ELEMENT = | GET FROM NUM_ARRAY AT I |
	> LET SUM = | SUM + CURRENT_ELEMENT |
	> LET I = | I + 1 |
	> END
> LET AVERAGE = | SUM / ARR_LEN |
> PRINT AVERAGE

Output:

31
