- fix weird behavior:
	this one which only exists when executing it from_file
	```
	LET B = [12, 32]
	LET A = | B |
	```

- fix while loop bug (verine that is in account for incrementing the index gets evaluated just once)

- FLOAT_FROM keyword
