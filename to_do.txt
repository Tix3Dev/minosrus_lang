- fix weird behavior:
	```
	LET A = 12
	IF A == 12 START
		PRINT "HELLOOOOOOO"
		ELSE
		PRINT "HELLO"
		END
	```

- update error messages (there are many missing)
	-> maybe automate the process

- fix while loop bug (verine that is in account for incrementing the index gets evaluated just once)
