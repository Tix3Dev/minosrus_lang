- fix weird behavior:
	```
	LET A = 12
	IF A == 12 START
		PRINT "HELLOOOOOOO"
		ELSE
		PRINT "HELLO"
		END
	```

- fix while loop bug (verine that is in account for incrementing the index gets evaluated just once)

(
- PUSH, POP, INSERT and REMOVE for strings
)
