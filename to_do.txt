- fix weird behavior:
	```
	LET A = 12
	IF A == 12 START
		PRINT "HELLOOOOOOO"
		ELSE
		PRINT "HELLO"
		END
	```

- indexing errors for e.g. REMOVE (like OUT OF BOUNDS or INVALID INDEX (-23))

- fix while loop bug (verine that is in account for incrementing the index gets evaluated just once)
