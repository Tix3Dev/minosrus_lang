( - maybe print help -> show where the error is
	e.g. > PRINT "HELLO"
		 -------------+
)

( - implement test cases (tokenizing) )

- show detailed error report when executing from file; all execution errors are "runtime errors"
